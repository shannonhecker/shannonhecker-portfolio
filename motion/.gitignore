node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Local symlink dir (per-machine paths into node_modules) used by
# the macOS 12 Remotion render workaround (ffmpeg-static binaries).
# See reference_remotion_macos12.md.
bin
